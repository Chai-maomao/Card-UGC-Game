# 联机提示、重连入口与新手教程验收标准

验收基线：Godot 4.6.3 stable，Windows，简体中文与英文均可正常显示。

## 1. 联机状态提示

- 战场底部不再向玩家显示“已同步给对方”“命令已发送/已接收”等实现细节。
- 正常等待对手、等待服务器确认或等待恢复时，只显示“等待中...”及必要的断线/重连进度。
- 操作重同步失败时给出可执行的提示“操作未完成，请重试”，不暴露协议术语。
- 单机、教程和联机的回合按钮、遮罩状态不受文案收敛影响。

自动验收：`TestMenuReconnectUx.tscn`、`TestOnlineEndTurn.tscn`、`TestNetworkSafety.tscn`。

## 2. 首页重连入口

- 只有存在通过校验且未失效的房间会话或局域网会话时，首页才显示“继续对局”。
- “继续对局”与首页主按钮使用同一套尺寸、圆角、字体、焦点、悬停和按下反馈。
- 点击后直接调用现有 `NetworkManager.begin_saved_match_reconnect()`，不建立第二套重连状态机。
- 重连期间按钮禁用并显示明确进度；失败后恢复为可重试状态，不会重复并发连接。
- 服务端房间重连和局域网主机重连都能在断线及客户端重启后恢复到权威快照。

自动验收：`TestMenuReconnectUx.tscn`、服务器重连集成测试、局域网重连集成测试。

## 3. 引导式新手教程

- 首页有稳定可见的“新手教程”入口，进入后使用真实战斗状态和真实操作路径。
- 教程依次引导：认识界面、召唤、使用技能、选择攻击者、选择目标、结束回合、完成。
- 每一步使用聚焦框和简短指令指出当前交互目标；与当前步骤无关的战斗操作会被拦截并给出视觉反馈。
- 教程可随时跳过或退出；完成/跳过状态持久化，但入口始终保留以便重玩。
- 教程始终保持玩家视角，完成后可返回首页，不污染普通单机或联机准备状态。
- 1280×720 与 1152×648 下，教程面板、聚焦框、手牌、状态条和主要操作按钮无不可接受的遮挡或越界。

自动验收：`TestTutorialFlow.tscn`、`TestResponsiveUi.tscn`；视觉验收需使用非 headless 客户端截图。

## 4. 交付门槛

- 全部 `Test*.tscn` 测试通过，无新增脚本解析错误。
- 服务端房间和局域网两条重连集成测试通过。
- 非 headless 检查首页、教程说明阶段和实战引导阶段；发现的遮挡或样式问题完成回归。
- 生成可运行的 Windows 客户端交付包，并记录版本与路径。
