# 完整创卡式新手教程验收标准

验收环境：Godot 4.6.3 stable；1280×720 基准窗口，1152×648 响应式回归。

## 1. 技能积木可读性与引导框

- 教程中非当前步骤的积木不可执行，但继续保留原有分类色、文字色和轮廓，不使用低对比度灰色替代。
- 点击或拖动非当前积木不得改变技能脚本，只提示当前教学目标。
- 玩家滚动积木库、自动定位目标或改变窗口尺寸时，高亮框逐帧贴合被引导积木；目标滚出可视区域时，高亮框按滚动容器边界裁切或隐藏，不悬停在旧位置。

## 2. 完整卡牌创建流程

- 战斗中心的“新手教程”先进入真实 `CardEditor.tscn`，固定使用随从类型，依次覆盖可选卡面、名称与种类、费用/生命/攻击、实时卡牌预览、第一技能、技能后的整卡复核与确认。
- 技能步骤进入真实 `SkillEditor.tscn`，完成名称、召唤时触发、抽牌积木、技能预览和保存，再返回同一张教程卡的整卡编辑器。
- 每一步只允许当前需要的真实控件产生修改；退出或跳过不写入牌库、不覆盖草稿恢复文件，并恢复进入教程前的草稿和编辑上下文。
- 最终确认走现有 UGC 安全校验并生成真实 `CardData`；名称、费用、生命、攻击、种类、卡面路径与技能数据与教程草稿一致。

## 3. 教程牌组与首回合发牌

- 教程玩家牌组严格由 `CardDatabase.player_starters()` 的一套初始牌加一张本次教程卡组成；教程卡只出现一次。对手使用一套确定性初始牌组。
- 战场教程开始时，玩家手牌必定包含且优先展示本次教程卡，不依赖随机洗牌；共享牌堆不得再含第二张教程卡。
- 战场引导的召唤、技能、攻击和结束回合全部调用现有真实战斗逻辑；教程卡实际消耗玩家设置的费用并执行玩家创建的召唤抽牌技能。

## 4. 引导结束后的分支

- 完成操作引导后明确提供“继续完成本局”和“退出教程”两个选择，位置不会因步骤变化互换。
- 退出教程清理临时战斗准备并返回战斗中心；继续本局移除全部教学操作限制，保留当前场面、手牌、牌堆、血量和回合状态。
- 继续后切换为正常练习对战，AI 能在其回合行动，玩家可继续操作并触发正常胜负结算。

## 5. 交付门槛

- 新增完整创卡、跨编辑器交接、教程牌组唯一性、首回合入手、滚动高亮跟随和继续战斗分支自动测试。
- 所有既有 `Test*.tscn`、服务器重连和 LAN 重连回归继续通过。
- 使用非 headless 客户端完成一次“创建卡牌→组装技能→确认整卡→拖拽召唤→完成引导→继续本局”的真实视觉与交互检查。
