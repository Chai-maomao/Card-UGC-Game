# 战斗入口整合与完整新手教程验收标准

验收环境：Godot 4.6.3 stable，Windows，1280×720 基准窗口；1152×648 由响应式测试覆盖。

## 1. 首页战斗入口

- 首页只保留一个“进入战斗”主入口，不再散落“继续对局 / 开始战斗 / 联机对战 / 新手教程”四个并列按钮。
- 统一战斗面板内按优先级展示：可恢复对局（仅有效时）、本地战斗、联机对战、新手教程、返回。
- 存在有效续局时，首页主入口与面板内续局按钮都有明确但不过度打扰的状态提示。
- 续局按钮继续调用现有 `NetworkManager.begin_saved_match_reconnect()`；本地、联机和教程继续进入原有真实业务链路。
- 打开或退出战斗面板不会清除续局会话；只有玩家明确开始一场替代性新对局时才按现有规则处理旧会话。

## 2. 教程导航防误触

- 教程面板始终保留三个固定宽度、固定顺序的按钮槽：主操作、跳过教程、退出。
- 需要玩家完成实操时，主操作按钮保持原位并显示“请完成当前操作”，处于禁用状态；跳过按钮不得移动到主操作的位置。
- 完成页允许隐藏“跳过教程”的文字，但必须保留等宽占位，退出按钮也不得横移。
- 战场教程与技能编辑器教程使用相同的导航顺序、危险层级和视觉风格。

## 3. 技能编辑器实操教程

- 从首页启动新手教程后，先进入真实 `SkillEditor.tscn`，而不是模拟界面或纯文字页。
- 依次引导玩家：理解积木结构、输入技能名、选择“召唤时”触发、添加“抽牌”效果积木、检查预览并保存。
- 每一步只开放当前需要的真实控件，其他积木、模板和保存入口不可误操作；错误操作不能推进步骤。
- 保存必须通过现有技能编译校验，并形成包含名称、`on_summon` 触发及 `draw_cards` 效果的有效技能。
- 完成编辑器章节后自动进入真实战场章节；编辑教程不写入牌库、不覆盖草稿恢复文件，并在完成、跳过或退出时恢复玩家进入前的编辑状态。
- 教程支持退出、跳过、重玩；编辑器章节完成状态与整个教程完成状态均可持久化。

## 4. 交付门槛

- 新增首页战斗中心、固定导航和技能编辑器教程自动测试；全部 `Test*.tscn` 通过。
- 服务端房间重连与 LAN 自动发现/重启续局集成测试继续通过。
- 使用非 headless 客户端检查首页、战斗中心、技能编辑器各教学阶段和战场教学，实际完成至少一次技能积木点击/保存以及一次战场拖拽。
- 导出独立命名的 Windows 客户端包，冒烟测试退出码为 0。
